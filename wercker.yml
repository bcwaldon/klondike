box: google/golang
build:
  steps:
    # Sets the go workspace and places you package
    # at the right place in the workspace tree
    - setup-go-workspace

    # Build the project
    - script:
        name: go build
        code: |
          ./build

    # Test the project
    - script:
        name: go test
        code: |
          go test ./...
